# Only needed for the optional CLI route in scripts/deploy.sh.
# The test suite needs none of this: pytest tests/ -q runs offline.
# Keys stay in the GenLayer CLI's own keystore and never in a file here.
GENLAYER_NETWORK=studionet
